# DECISIONS — לוג החלטות (ADR)

> Append-only. לא עורכים רשומה קיימת. החלטה שמשתנה מקבלת רשומה חדשה שמפנה לישנה.

---

## ADR-001 · בנייה מחדש והעברת נתונים
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** בונים את המערכת מחדש על Next.js (App Router), Postgres (Neon) ו-Vercel, הסטאק של Nofar ו-SIM320. כל הטיסות הקיימות עוברות למערכת החדשה, והמערכת הישנה ממשיכה לעבוד עד המעבר.
**למה:** `app.js` מונוליטי של 211KB עם פונקציה שמוגדרת פעמיים וקבצים מתים. יש שני סטים סותרים של מחירי ברירת מחדל, וסכמת Postgres לשרת שרץ על MongoDB. שלושה מסלולים שונים מחשבים רווח, והם התפצלו זה מזה (5 באגים תוקנו ב-7841342).
**נדחה:** שיפוץ הדרגתי במקום. גורר את כל החוב הטכני, והמשתמש רוצה לגעת כמעט בהכל.

## ADR-002 · מעקב בענן, חינמי, מחוץ למכסות Vercel
**תאריך:** 2026-09-25 · **סטטוס:** מאושר (השירות עצמו פתוח, Q3)
**החלטה:** מנוע המעקב רץ בשרת בענן ולא בדפדפן. אילוצים: **חינם**, ו**לא צורך מכסות של Vercel**. הוא דוגם את VATSIM רק כשיש טיסה פעילה.
**למה:** VATSIM מספק רק תמונת מצב נוכחית בלי היסטוריה, אז חייבים לדגום לאורך כל הטיסה. מעקב בדפדפן נעצר כשהטאב נסגר או כשהאייפד נרדם.
**נדחה:** דפדפן בלבד (שביר); מעקב משולב (שני מנגנונים לתחזק).

## ADR-003 · לוגבוק VATSIM בלבד
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** רק טיסה שזוהתה ונעקבה ב-VATSIM נכנסת ללוגבוק. אין יצירת טיסה ידנית ואין טיסות אופליין.
**למה:** נתונים נקיים ואמינים. זמנים אמיתיים במקום ידניים.
**השלכה פתוחה:** הצגת הטיסות ההיסטוריות שלא נעקבו (Q5).

## ADR-004 · מכשירים
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** המחשב ליד הסים הוא הראשי (Desktop-first). אייפד וטלפון נתמכים באופן מלא. כל מוקאפ מוצג בשלושת הגדלים.

## ADR-005 · מודל כלכלי: הכנסות פחות הוצאות
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** המערכת ממשיכה לדמות חברת תעופה. כל טיסה מקבלת הכנסות (כרטיסים ומטען) והוצאות, ובסוף רווח או הפסד.
**נדחה:** מעקב הוצאות בלבד, בלי הכנסות.

## ADR-006 · מנוע חישוב אחד, דטרמיניסטי
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** כל החישובים עוברים דרך מודול אחד. לכל סעיף יש נוסחה אחת. **אסורה אקראיות.** לכל מספר יש מקור שאפשר להצביע עליו: SimBrief, VATSIM, הזנה ידנית או טבלת ההגדרות. כל הנוסחאות הקיימות נכתבות מחדש.
**למה:** היום עמלות נחיתה ושכבות מחירי הכרטיסים כוללות רעש אקראי של ±3% (`pricing.js:244`). יש גם נקודת קצה שמייצרת היסטוריית מחירים מזויפת (`pricing.js:28`). ובנוסף "תנאים על תנאים" בין כמה מסלולי חישוב גורמים לבאגים.

## ADR-007 · ספר חשבונות (ledger) לכל טיסה
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** בסגירת טיסה המנוע מחשב פעם אחת, וכל סעיף נשמר כשורה נפרדת: סוג, סכום, מקור ופרטי החישוב. דוחות וסטטיסטיקות **רק סוכמים שורות**. שינוי תעריפים לא משנה טיסות שכבר נסגרו.
**למה:** היום נשמר רק `profit` אחד לכל טיסה, ולכן כל דוח משחזר את העלויות בדרך משלו. זה שורש הבאגים בדוחות.

## ADR-008 · זמן בלוק אמיתי
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** זמני OUT/OFF/ON/IN שנמדדים ב-VATSIM מחליפים את הזמן המתוכנן ב-SimBrief בכל חישוב תלוי-זמן (צוות, תחזוקה, שעות טיסה).

## ADR-009 · אבני דרך אוטומטיות
**תאריך:** 2026-09-25 · **סטטוס:** מאושר (התוכן פתוח, Q6)
**החלטה:** אבני דרך מצטברות מחליפות את היעדים שהמשתמש מגדיר. אבן הדרך הבאה נפתחת לבד כשהקודמת הושגה.
**נדחה:** יעד חודשי שנגזר מההיסטוריה.

## ADR-010 · הסרת "משימות מיוחדות"
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** הטאב והמודול של המשימות לא עוברים למערכת החדשה.

## ADR-011 · סעיפי הוצאה: ידני מול אוטומטי
**תאריך:** 2026-09-25 · **סטטוס:** מאושר (הנוסחאות פתוחות)
**החלטה:**
- **ידני בסוף טיסה, בדולרים, מועתק מ-GSX Pro:** דלק, צוות קרקע, קייטרינג. וגם FPM.
- **אוטומטי:** טייסים ודיילים, תחזוקה, עמלות נחיתה ושדה, קנס נחיתה קשה, **הקפצת צוות** (חדש).
- ניקיון כסעיף נפרד יורד.
**נדחה:** משיכה אוטומטית מ-GSX Pro. דורשת תוכנה על מחשב הסים (WASM bridge, ראה פרויקט GSX Control) ויוצאת מארכיטקטורת הענן.

## ADR-012 · הקפצת צוות (Positioning)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר כסעיף (הנוסחה פתוחה, Q2)
**החלטה:** כשטיסה ממריאה משדה שהטיסה הקודמת לא נחתה בו, נרשמת הוצאה על הטסת הטייסים והצוות למוצא החדש.
**למה:** רצף ריאליסטי. חברה שקופצת בין שדות משלמת על זה.

## ADR-013 · Excel: ייצוא, וייבוא לשחזור בלבד
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** ייצוא ל-Excel נשאר. ייבוא מקבל רק קובץ שהמערכת עצמה ייצאה (שחזור גיבוי) ולא יוצר טיסות חדשות.
**למה:** ייבוא חופשי היה עוקף את ADR-003 (VATSIM בלבד).

## ADR-014 · הסרת כרטיס העלייה למטוס
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** החלון יורד. פרטי הטיסה בלוגבוק מכילים את כל המידע שלו ויותר (ציר זמן של שלבי הטיסה ופירוט הכסף).
**למה:** החלון מציג שוב נתונים שכבר קיימים, ומוסיף מסך. אם בעתיד יידרש שיתוף, הוא ייעשה ככפתור "תמונת שיתוף" בתוך פרטי הטיסה.

## ADR-015 · כיוון חזותי: תפעולי / EFB
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** שפת עיצוב של כלי תפעולי. מסך צפוף, מספרים בולטים, ערכים מיושרים בטורים, פינות של 4px, בלי צללים, בלי גרדיאנטים ובלי אימוג'י. צבע מופיע רק כשהוא אומר משהו: ירוק זה פעיל או רווח, כתום זה צפי או דורש תשומת לב, אדום זה הפסד, וכחול זה אינטראקציה או מסלול. גופן Heebo עם ספרות טבלאיות. מקור: `docs/mockups/s0-visual-direction.html`, כיוון 1.
**נדחה:** אוויוניקה / לוח טיסות (כיוון 3): כהה בלבד, וגופן מונו פחות נוח לעיון בסטטיסטיקות. נקי ומינימלי (כיוון 2): לא הוצג, כי מעט מספרים במסך סותר את הדרישה לכמה שפחות מסכים.

## ADR-016 · ערכת צבעים לפי המכשיר
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** בהיר וכהה, לפי ההגדרה של מערכת ההפעלה, עם אפשרות להחליף ידנית.

## ADR-017 · עברית בלבד
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** הממשק בעברית בלבד. מונחים תעופתיים נשארים באנגלית כמו שהם: ICAO, OUT/OFF/ON/IN, FPM, FL, kt.
**למה:** הממשק הדו-לשוני היום מכפיל כל טקסט, ובאגי כיווניות בין עברית לאנגלית חוזרים על עצמם.

## ADR-018 · מיקום הקוד (סוגר את Q8)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** אותו ריפו (`maor561/SmartLogBook`) ואותו פרויקט Vercel. העבודה נעשית בענף `makeover`, ובמעבר ממזגים ל-`main`, כך שהכתובת לא משתנה.

## ADR-019 · VATSIM קודם, וידני כגיבוי (מחליף את ADR-003)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מחליף:** ADR-003
**החלטה:** כל טיסה מתחילה מתוכנית ב-SimBrief, ובלי תוכנית אין טיסה. המעקב דרך VATSIM הוא הדרך הרגילה, אבל יש שני מצבי גיבוי:
- **טיסה ידנית:** יש תוכנית ב-SimBrief ואין חיבור ל-VATSIM. מקרה נדיר. אתה מזין את ארבעת זמני הבלוק (OUT/OFF/ON/IN).
- **השלמה ידנית:** החיבור ל-VATSIM היה חלקי. אתה משלים רק את הזמנים שחסרים.
לכל טיסה נשמר **מקור**: `נעקבה` / `חלקית` / `ידנית`. המקור מוצג בלוגבוק וזמין כמסנן בסטטיסטיקות. כל הטיסות נספרות בכל החישובים.
**למה:** המשתמש ביקש במפורש. ניתוק מ-VATSIM או טיסה בלי חיבור הם מצבים אמיתיים, וטיסה כזו לא צריכה ללכת לאיבוד.
**נשמר מ-ADR-003:** תוכנית SimBrief היא חובה. ייבוא מ-Excel עדיין לשחזור בלבד (ADR-013).

## ADR-020 · כללי מחזור החיים של טיסה (סוגר את Q4)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר (הספים המספריים, כמו מהירות וגובה לזיהוי שלבים, ייקבעו בשלב הארכיטקטורה)
**החלטה:**
- **תוכנית שלא נטסה:** תוכנית חדשה ב-SimBrief מחליפה אותה. אם לא נוצרה תוכנית חדשה ואין חיבור, היא פגה אחרי 12 שעות. לא נשאר עליה שום רישום.
- **ניתוק באוויר:** המערכת מחכה 30 דקות לחיבור מחדש. אם החיבור חוזר, זו אותה טיסה והמעקב ממשיך. אחרת הטיסה מסומנת `קטועה`, ואתה בוחר בין השלמה ידנית (ADR-019) למחיקה.
- **הסטה:** הטיסה נרשמת לשדה שנחתת בו בפועל, עם תג `הסטה`, **ועם הוצאת הסטה** (העברת הנוסעים ליעד המקורי). הנוסחה פתוחה (Q2). הטיסה הבאה ממשיכה מהשדה שבו נחתת.
- **טופס ההשלמה** (עלויות GSX ו-FPM) נפתח רק ב-**IN**, כלומר עצירה בגייט או ניתוק אחרי נחיתה.

## ADR-021 · עריכת טיסה סגורה: רק מה שהוזן ידנית (מרחיב את ADR-007)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** בטיסה סגורה מותר לערוך רק שדות שהוזנו ידנית: סכומי GSX, FPM, וזמנים שהוזנו ידנית בטיסה חלקית או ידנית. זמנים שנמדדו ב-VATSIM נעולים. אחרי עריכה, ספר החשבונות **של אותה טיסה בלבד** מחושב מחדש, **לפי צילום התעריפים שנשמר ביום הסגירה**, והטיסה מסומנת `נערכה` עם תאריך.
**השלכה על מודל הנתונים:** כל טיסה שומרת את התעריפים שבהם נסגרה. בלי זה, עריכה הייתה מושכת את התעריפים של היום.
**נדחה:** נעילה מלאה (טעות הקלדה אחת הייתה מחייבת למחוק טיסה שלמה). עריכה חופשית של זמני VATSIM (פוגעת באמינות של טיסה שמסומנת "נעקבה").

## ADR-022 · מסלול בפועל לא נשמר, ומפת המסלולים עוברת ללוגבוק
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** נקודות המיקום מ-VATSIM משמשות רק לזיהוי שלבים ולמעקב החי, ונמחקות כשהטיסה נסגרת. נשמרים רק זמנים ונתונים. מפת המסלולים היא **תצוגה חלופית בלוגבוק** (מתג "רשימה / מפה"), והיא מציירת קו בין המוצא ליעד של כל טיסה שעוברת את הסינון.

## ADR-023 · סינון הלוגבוק
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** אפשר לסנן לפי תקופה, שדה (מוצא או יעד), סוג מטוס, מקור (נעקבה / חלקית / ידנית), ותגים (הסטה, הקפצת צוות, נחיתה קשה, הפסד). יש גם חיפוש חופשי: אות קריאה, ICAO, רישום. אותו סינון חל גם על מפת המסלולים וגם על ייצוא ה-Excel.

## ADR-024 · מנוע המעקב: Cloudflare Worker ו-D1 (סוגר את Q3, מממש את ADR-002)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:**
- **Worker בתוכנית החינמית של Cloudflare, עם Cron Trigger שרץ כל דקה.** בכל הרצה הוא מוריד את הפיד של VATSIM ומחפש בו את ה-CID בחיפוש ממוקד, בלי לפענח את כל הקובץ.
  - מחובר ואין טיסה פעילה: מושך את ה-OFP מ-SimBrief פעם אחת ומתאים (אות קריאה, מוצא, יעד).
  - יש טיסה פעילה: מריץ את מכונת המצבים (שלבים, OUT/OFF/ON/IN, ניתוק).
- **מצב המעקב נשמר ב-D1** (SQLite של Cloudflare, חינם). הכתיבה נעשית רק כשמשהו משתנה, ועוד חותמת "נראה לאחרונה".
- **המסך החי באפליקציה קורא ישירות מה-Worker** (CORS), ולא דרך Vercel.
- **Neon, מסד הנתונים הראשי, נכתב רק מהאפליקציה**, כשאתה שולח את טופס ההשלמה. ה-Worker לא נוגע בו.
- **כלל:** אם הורדת הפיד של VATSIM נכשלת, זו "תקלת פיד" ולא ניתוק של הטייס. היא לא נספרת ב-30 הדקות של ADR-020.
- **דיוק הזמנים: ±דקה אחת** (לפי קצב הדגימה).

**נתונים שאומתו (2026-09-25):**
- **Workers חינמי:** 100,000 בקשות ביום, 10ms זמן מעבד להרצה, עד 5 טריגרים של Cron.
- **D1 חינמי:** 100,000 כתיבות ביום, 5M קריאות, 5GB.
- **Neon חינמי:** 100 שעות חישוב בחודש, והשהיה אוטומטית אחרי 5 דקות. לכן אסור שהמעקב יעיר אותו כל דקה.
- **פיד VATSIM:** מתעדכן כל 15 שניות. נמדד: 1.16MB, 1,084 טייסים. פענוח מלא לוקח 2.2ms, חיפוש ממוקד לוקח 0.4ms.
- **OFP של SimBrief:** נמדד 1.7MB, ושדות המשקל והמושבים (`max_tow`, `max_ldw`, `oew`, `max_passengers`, `cargo`) קיימים. זה מאמת את העיקרון "כל מטוס עובד" מה-Brief, §5.

**צפי שימוש:** כ-2% ממכסת הבקשות של Workers. אפס מ-Vercel.
**סיכון שנבדק ראשון בבנייה:** זמן המעבד האמיתי של הרצה ב-Cloudflare (מול תקרת 10ms), בזמני עומס של VATSIM.
**פעולה של המשתמש:** לפתוח חשבון Cloudflare חינמי, בשלב הבנייה.
**נדחו (ADR-024):** Vercel Cron (מכסות, ובתוכנית החינמית פעם ביום). GitHub Actions (מרווח של 5 דקות לפחות, ומתעכב). Supabase pg_cron (ספק שני, והשהיה אחרי שבוע). שרת חינמי ב-Oracle (תחזוקת שרת). Slurper של VATSIM (בלי גובה ומהירות).

## ADR-025 · מחיר כרטיס ותעריף מטען דינמיים (סוגר את Q1)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **סקיצה:** `docs/mockups/s2-dynamic-pricing.html`
**החלטה:** המחיר מחושב אוטומטית, ננעל ב-OUT, ונרשם בספר החשבונות שלב אחר שלב.
- **בסיס:** `F0 + K × NM^0.65`. ברירות המחדל: F0 = $40, K = 2.8. המרחק מה-OFP.
- **מכפילים:**
  - תפוסה: `0.85 + 0.30 × LF²`, כאשר LF = `pax_count / max_passengers`.
  - עונה לפי חודש: ×0.85 עד ×1.20.
  - יום בשבוע: ×0.90 עד ×1.08.
  - שעה מקומית: ×0.90 עד ×1.05. אזור הזמן לפי `times.orig_timezone` ב-OFP.
- **מעקה:** מכפלת המכפילים מוגבלת לטווח ×0.70–×1.40 מול הבסיס.
- **תוספת דלק:** `0.35 × (מחיר EIA / מחיר ייחוס − 1)`, מוגבלת בין −10% ל-+15%. מחיר הייחוס $0.75/ק״ג. אם EIA לא זמין, משתמשים בערך האחרון שנשמר, **לא במספר אקראי**.
- **מטען:** `(0.8 + 0.05 × NM^0.55)` $/ק״ג × (1 + תוספת הדלק).
- **המועד הקובע:** זמן ה-OUT בפועל. בטיסה ידנית, ה-OUT שהוזן.
- **שינוי טבלאות ופרמטרים בהגדרות** חל רק מהטיסה הבאה (ADR-007, ADR-021).
- **מוניטין החברה** כגורם של ±5%: נדחה לשלב 5 (Q6).
**נדחה:** טבלת שכבות לפי מרחק (קפיצות ב-500 וב-2000 NM). נוסחת השרת הנוכחית (שכבות עם רעש אקראי).

## ADR-026 · נוסחת הקפצת צוות (סוגר את Q2, חלק א')
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מפרט את:** ADR-012
**החלטה:**
- **מתי:** כשמוצא הטיסה שונה מ**מיקום הצוות**, כלומר השדה שבו נחתה בפועל הטיסה הקודמת (כולל נחיתת הסטה).
- **מי:** כל הצוות. 2 טייסים, ודיילים לפי מספר המושבים: `ceil(max_passengers / 50)`, מתוך ה-OFP. ב-737 עם 189 מושבים זה 6 אנשי צוות.
- **כמה:** `אנשי צוות × מחיר בסיס (ADR-025) למרחק בין מיקום הצוות למוצא החדש`. בלי מכפילים ובלי לינה.
- **מרחק קצר מ-50 NM:** חינם, כי זו נסיעה בין שדות באותה עיר.
- **הטיסה הראשונה:** מיקום הצוות הוא השדה שבו נחתה הטיסה האחרונה שהועברה מהמערכת הישנה. אם אין טיסות בכלל, זה **שדה הבית** שמוגדר בהגדרות (ברירת מחדל LLBG).
**נדחה:** טייסים בלבד. תוספת לינה. סכום קבוע שלא תלוי במרחק.

## ADR-027 · נוסחת הוצאת הסטה (סוגר את Q2, חלק ב')
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מפרט את:** ADR-020
**החלטה:** `נוסעים × מחיר בסיס (ADR-025) למרחק בין שדה ההסטה ליעד המקורי`. בלי מכפילים. אם המרחק קצר מ-50 NM, משלמים סכום קבוע לנוסע על הסעה באוטובוס (הגדרה, ברירת מחדל $25).
**השלכה על הארכיטקטורה:** VATSIM לא אומר באיזה שדה נחתת, ולשדה הסטה שאינו החלופי אין נתונים ב-OFP. לכן צריך **מאגר שדות** (קוד ICAO וקואורדינטות) כדי לזהות את השדה לפי מיקום הנחיתה ולחשב מרחקים. מועמד: OurAirports, נתונים פתוחים וחינמיים. ייסגר בשלב הארכיטקטורה.
**נדחה:** החזר של אחוז קבוע מהכנסת הכרטיסים (לא תלוי במרחק של ההסטה). סכום קבוע לנוסע.

## ADR-028 · נוסחאות ההוצאות האוטומטיות (סוגר את Q9)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **סקיצה:** `docs/mockups/s3-cost-engine.html`
**החלטה:** כל ההוצאות האוטומטיות נגזרות מ-MTOW (`max_tow`, בטונות) ומהמושבים (`max_passengers`) שב-OFP, ומזמני הבלוק והאוויר שנמדדו. אין רשימה קשיחה של מטוסים.

**טייסים ודיילים:**
- נוסחה: `max(block, 2h) × (200 + 120 + 120 × טייסים נוספים + 40 × ⌈seats/50⌉)` $.
- מינימום של 2 שעות תשלום לכל טיסה.
- מעל 8 שעות בלוק: טייס שלישי. מעל 12 שעות: רביעי.

**תחזוקה:** `air × (450 + 10.8 × MTOW) + 3 × MTOW`. לפי שעת אוויר (OFF→ON), ועוד עלות לכל המראה ונחיתה.

**עמלות נחיתה ושדה:** `7 × MTOW + 12 × pax`.

**דמי ניווט אווירי (חדש):** `70 × (km / 100) × √(MTOW / 50)`. זו נוסחת Eurocontrol.

**חכירת מטוס (חדש):** `block × 10.5 × MTOW`. ב-737-800 זה כ-$830 לשעה.
**אומת:** חכירה חודשית של 737-800 בגיל ביניים היא כ-$228K (2026), והניצולת כ-9 שעות בלוק ביום, כלומר כ-275 שעות בחודש. ‏$1,000 לשעה (13 × MTOW) נבדק ונדחה, כי הוא מתאים למטוס חדש יותר.

**קנס נחיתה קשה (לפי FPM בערך מוחלט):**
- עד 400: $0.
- 401–600: `15 × MTOW` (בדיקה ויזואלית).
- 601–900: `60 × MTOW` (בדיקת נחיתה קשה לפי AMM).
- מעל 900: `250 × MTOW` (בדיקה מבנית).

**כל המקדמים** ניתנים לשינוי בהגדרות, והשינוי חל מהטיסה הבאה (ADR-021).
**נדחה:** התחזוקה של היום ($3,000 לשעה, רשימה של 10 מטוסים ומכפיל מטען), וקנס קבוע של $500 או $1,000.

## ADR-029 · כיול הרווחיות: K = 1.04, יעד 20% (מעדכן את ברירת המחדל של ADR-025)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מעדכן:** ADR-025 (K=2.8 → 1.04)
**החלטה:** מקדם המרחק במחיר הבסיס יורד מ-2.8 ל-**1.04** (F0 נשאר $40). **יעד השוליים הוא 20%.**
**איך כוילו:** המנוע (ADR-025 + ADR-028) הורץ על **10 הטיסות האמיתיות האחרונות** של המשתמש (737-800, 179–2,240 NM) עם סכומי ה-GSX שהזין.
- הגורמים שלא ידועים בדיעבד, שעה מקומית ותוספת דלק, נקבעו לנייטרלי.
- בדיקה בינארית על K מצאה: 10% → 0.81, **20% → 1.04**, 30% → 1.33.
- ב-K=2.8 השוליים היו 57%, והכרטיסים יקרים פי 2 מהמציאות (קלן–ליסבון $349).
**ממצא:** עם עלויות GSX אמיתיות, השוליים אחידים לאורך כל המרחקים (53%–62% ב-K=2.8). כלומר המבנה מאוזן ורק הגובה כויל. בכיול ל-20% גם מחירי הכרטיסים יוצאים ריאליסטיים (תל אביב–קטניה כ-$158).
**בהמשך:** לחזור על הכיול אחרי כ-20 טיסות במערכת החדשה, כשיהיו זמני בלוק אמיתיים, שעה מקומית ותוספת דלק מ-EIA.

## ADR-030 · העברת הטיסות הקיימות: "היסטוריות", בלי חישוב מחדש (סוגר את Q5)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** 50 הטיסות מ-MongoDB עוברות כמו שהן, עם מקור `היסטורית` (סוג רביעי, בנוסף ל-ADR-019).
- **ספר החשבונות של כל טיסה כזו הוא שורה אחת:** `legacy_profit`, עם הרווח שנשמר במערכת הישנה. לא מחושבים בדיעבד סעיפים ולא ממציאים נתונים (ADR-006).
- **הזמן שנשמר במערכת הישנה** (`duration_mins`, זמן אוויר מתוכנן מ-SimBrief) עובר כ"זמן אוויר מתוכנן" ומסומן ככזה. זמני OUT/OFF/ON/IN נשארים ריקים.
- **סכומי GSX שקיימים ב-24 טיסות** נשמרים כנתוני מקור לעיון, אבל לא נכנסים לספר החשבונות.
- **בסטטיסטיקות:** הטיסות האלה נספרות בטיסות, שעות, נוסעים, מסלולים ואבני דרך. בניתוח הכספי הן מופיעות כשורת "רווח מהמערכת הישנה", ואפשר להוציא אותן בלחיצה. התג שלהן בלוגבוק: `היסטורית`.
- **הטיסה האחרונה שעוברת** קובעת את מיקום הצוות ההתחלתי (ADR-026).
- **המסמך המקורי** מ-MongoDB נשמר כמו שהוא בעמודת JSON, לעיון ולשחזור.
**נדחה:** חישוב מחדש של 24 הטיסות עם GSX (זמני בלוק מומצאים). להתחיל מאפס.

## ADR-031 · התחברות בסיסמה, משתמש יחיד (סוגר את Q11)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:**
- **משתמש יחיד, כניסה בסיסמה.** הסיסמה נשמרת כ-hash (argon2 או bcrypt), ולא בטקסט גלוי.
- **Session ארוך:** 90 יום בכל מכשיר, כדי שבאייפד ובטלפון לא תצטרך להיכנס כל פעם.
- **הגבלת ניסיונות כניסה.**
- **כל ה-API מוגן**, בלי יוצאים מן הכלל. זה סוגר את החור של היום, שבו `/api/flights` פתוח לכולם.
- **ה-Worker של המעקב (ADR-024) גם מוגן:** האפליקציה מנפיקה טוקן חתום קצר-מועד (HMAC עם סוד משותף ל-Vercel ול-Cloudflare), וה-Worker מאמת אותו.
**נדחה:** Google (המשתמש העדיף סיסמה). קישור במייל (דורש שירות שליחת מיילים).

## ADR-032 · מטוסים: סוג ורישום בכל טיסה, בלי ניהול צי
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** כל טיסה שומרת את סוג המטוס, הרישום, המושבים והמשקלים מה-OFP. סטטיסטיקות לכל מטוס (לפי רישום) נגזרות מהטיסות. **אין לצי מיקום:** המטוס זמין בכל שדה, כמו בסים. רק הצוות מוקפץ (ADR-026).
**נדחה:** צי מלא עם מיקום לכל מטוס וטיסות מעבר (ferry).

## ADR-033 · מאגר שדות: OurAirports (סוגר את Q10)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:**
- **מקור:** נתוני OurAirports (פתוחים וחינמיים), מסוננים לשדות עם קוד ICAO, בערך 10,000 שדות.
- **מה נשמר:** ICAO, שם, עיר, מדינה, קואורדינטות וגובה, בטבלת `airports` ב-Neon.
- **שימושים:**
  - זיהוי שדה הנחיתה בפועל: השדה הקרוב ביותר למיקום ה-ON שמדד ה-Worker. אם הוא שונה מהיעד, זו הסטה.
  - חישוב מרחקים (Great Circle) להקפצת צוות ולהסטה.
  - שמות שדות בעברית או באנגלית במסכים.
- **מתי מזהים:** באפליקציה, כשהטיסה מגיעה ל-IN. ה-Worker שומר רק קואורדינטות ולא מחזיק את המאגר.
- **עדכון:** ייבוא ידני מדי פעם (סקריפט). שדות לא משתנים הרבה.

## ADR-034 · מטען = `weights.cargo` מה-OFP, וכיול מתוקן: K = 1.51 (מתקן את ADR-029)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מתקן:** ADR-029 (הערך של K) ו-ADR-025 (מקור המטען)
**הבעיה:** המערכת הישנה, ובעקבותיה הכיול ב-ADR-029, חישבה מטען כ-`payload − pax × 95`. אבל ב-SimBrief נוסע עם מזוודה שוקל 111 ק"ג (`pax_weight` 86 + `bag_weight` 25), ולכן **המזוודות נספרו כהכנסת מטען**. ב-10 טיסות הכיול זה יצא 25,713 ק"ג "מטען", כשהמטען האמיתי היה 93 ק"ג.
**החלטה:**
- **כמות המטען נלקחת ישירות מ-`weights.cargo` ב-OFP.** אסור לגזור אותה מה-payload.
- **יעד השוליים נשאר 20%**, זו ההחלטה של המשתמש ב-ADR-029. כשמריצים את הכיול מחדש עם המטען הנכון, **K = 1.51** (לעומת 1.29 ליעד 10% ו-1.80 ליעד 30%). F0 נשאר $40.
- **השפעה על המחיר:** תל אביב–קטניה עולה מכ-$158 לכ-$208. זה עדיין בטווח הריאליסטי.
**בהמשך:** הכיול החוזר (ADR-029) ייעשה על נתוני `weights.cargo` אמיתיים.

## ADR-035 · מסך הניתוח: 11 אזורים (סוגר את Q6, חלק א')
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **סקיצה:** `docs/mockups/s5-analysis.html`
**החלטה:** כל 11 האזורים מהסקיצה נכנסים:
1. מדדי מפתח.
2. רווח והפסד לפי סעיף, עם מדדי יחידה (RASM, CASM, רווח לשעת בלוק ולטיסה).
3. מגמה.
4. **עלויות קרקע לפי שדה** (מנתוני GSX).
5. מסלולים.
6. דיוק ותפעול.
7. איכות נחיתות.
8. מטוסים לפי רישום.
9. רשת ואיכות נתונים.
10. אבני דרך.
11. דירוג ודרגה.

**כללים:**
- **מקור הנתונים:** כל הנתונים הכספיים הם `SUM`/`GROUP BY` על `ledger_lines` (ADR-007).
- **בורר תקופה:** חודש, רבעון, שנה, הכל, מותאם.
- **טיסות היסטוריות:** מתג "כולל טיסות היסטוריות" (ADR-030).
- **הדפסה:** "הדפס כדוח" מחליף את הדוח החודשי.
- **גרפי זמן:** הציר מתחיל מימין, ותקופות בלי טיסות מוצגות כאפס (כמו בגרפי ה-KPI הקיימים).
- **יציאה בזמן (OTP):** OUT בפועל עד 15 דקות אחרי ה-OUT המתוכנן.

## ADR-036 · דירוג החברה: 4 עמודים (סוגר את Q6, חלק ב')
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:** הדירוג מחושב על **30 הטיסות הסגורות האחרונות**. כל עמוד מקבל ציון בין 0 ל-5, באינטרפולציה לינארית עם גבולות, והציון הכולל הוא הממוצע של ארבעתם.
- **בטיחות:** 70% לפי FPM ממוצע בערך מוחלט (עד 120 → 5, מעל 450 → 0), ו-30% לפי שיעור הנחיתות הקשות (0% → 5, מעל 20% → 0).
- **דיוק:** אחוז היציאות בזמן (90% ומעלה → 5, 40% ומטה → 0). טיסות היסטוריות לא נספרות, כי אין להן זמנים.
- **רווחיות:** שולי רווח (30% ומעלה → 5, 0% ומטה → 0). ביעד של 20% זה יוצא 3.3.
- **יעילות:** 70% לפי תפוסה (90% ומעלה → 5, 50% ומטה → 0), ו-30% לפי שיעור הטיסות עם הקפצה או הסטה (0% → 5, 25% ומעלה → 0).

**תצוגה:** לחיצה על עמוד מראה איך הציון שלו חושב (ADR-006: לכל מספר יש מקור).
**התחלה:** עם פחות מ-5 טיסות סגורות, הדירוג "בבנייה".

## ADR-037 · גורם מוניטין במחיר הכרטיס: ±5% (משלים את ADR-025)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**החלטה:**
- **נוסחה:** `מחיר × (1 + 0.05 × (דירוג − 3) / 2)`. כלומר 5 כוכבים → +5%, 3 → 0, 1 → −5%, וההשפעה מוגבלת ל-±5%.
- **איזה דירוג:** הדירוג הכולל בזמן ה-OUT, לפני הטיסה הנוכחית.
- **מיקום בחישוב:** צעד נפרד, אחרי מעקה המכפילים ולפני תוספת הדלק. נרשם בספר החשבונות, בשדה `calc`, כמו כל גורם.
- **כשהדירוג "בבנייה":** נייטרלי.
**למה:** טיסה טובה שווה כסף גם בטיסות הבאות. זה נותן לדירוג משמעות אמיתית.

## ADR-038 · דרגות לפי שעות בלוק, ואבני דרך (סוגר את Q6)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מפרט את:** ADR-009
**דרגות (לפי סך שעות הבלוק):**

| שעות | דרגה |
|---|---|
| 0 | טייס משנה |
| 100 | טייס משנה בכיר |
| 300 | קברניט |
| 750 | קברניט בכיר |
| 1,500 | קברניט בוחן |
| 3,000 | טייס ראשי |

טיסות היסטוריות נספרות לפי זמן האוויר המתוכנן שלהן (ADR-030).

**אבני דרך** (מצטברות, והבאה בכל קטגוריה נפתחת לבד):

| קטגוריה | מדרגות |
|---|---|
| שעות בלוק | 10 · 25 · 50 · 100 · 250 · 500 · 1,000 · 2,500 · 5,000 |
| טיסות | 10 · 25 · 50 · 100 · 250 · 500 · 1,000 |
| נוסעים | 1K · 5K · 10K · 25K · 50K · 100K · 250K |
| מרחק (NM) | 10K · 25K · 50K · 100K · 250K · 500K |
| רווח מצטבר | $100K · $250K · $500K · $1M · $2.5M · $5M · $10M |
| שדות שונים | 10 · 25 · 50 · 100 · 200 |
| רצף נחיתות רכות (עד 200 FPM) | 5 · 10 · 25 · 50 |

**נתונים:** אבן דרך שהושגה נשמרת בטבלה `milestones`, עם תאריך והטיסה שבה הושגה. בהעברת הנתונים מחשבים את כל אבני הדרך שכבר הושגו.

## ADR-039 · הכנסת מטען = `weights.freight_added` (מתקן את ADR-034)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מתקן:** ADR-034 (שם השדה בלבד)
**ממצא ב-WP0:** הבדיקה `tools/check-simbrief.mjs` הורצה על OFP אמיתי והראתה ש-`weights.cargo` **כולל את המזוודות**:
- `payload` 20,424 = `pax_count` 184 × `pax_weight` 86 + `cargo` 4,600.
- `cargo` 4,600 = `bag_count` 184 × `bag_weight` 25.
- `freight_added` = 0.

**החלטה:**
- **המטען שמכניס כסף הוא `weights.freight_added`.**
- **הבדיקה במנוע:** `cargo − bag_count × bag_weight` חייב להיות שווה ל-`freight_added`. אם הם שונים, מסמנים את הטיסה לבדיקה ולא מנחשים.
**הכיול לא משתנה:** ב-ADR-034 המטען חושב כ-`payload − pax × 111`, וזה שווה בדיוק ל-`freight_added` כשמספר המזוודות שווה למספר הנוסעים. K = 1.51 נשאר.

## ADR-040 · תוצאת WP0/1: זמן המעבד ב-Cloudflare (מאשר את ADR-024)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר
**מדידה:** ה-Worker `smartlogbook-tracker` נפרס לחשבון של המשתמש, עם cron כל דקה ו-CID 1242058. בשלוש הרצות מתוזמנות רצופות (`wrangler tail --format json`) נמדד:
- **cpuTime: 2–3ms**, מתוך תקרה של 10ms בתוכנית החינמית.
- **wallTime: 148–180ms.** זה בעיקר המתנה לרשת, והיא לא נספרת.

הפיד היה בגודל 1,333KB, וזה המקרה הנפוץ: אין טיסה פעילה, כלומר סריקה מלאה בלי למצוא. **יש מרווח של פי 3 עד 5 מהתקרה.** לשם השוואה, המדידה המקומית של החיפוש לבד הייתה 0.29ms, כך שרוב זמן המעבד בענן הוא קריאת גוף התגובה.
**החלטה:** ממשיכים עם הארכיטקטורה של ADR-024 כמו שהיא.
**מעקב:** לבדוק שוב ב-WP4 בשעות עומס ובאירועים של VATSIM, כשהפיד גדול יותר. אם מתקרבים ל-7ms, עוברים לקריאה בזרם (stream) שעוצרת כשמוצאים את ה-CID (WORK_PACKAGES, "החלטות שעוד ייפתחו").
**בדרך אומת גם:** ה-OFP האחרון של המשתמש (`MAOR561`, ELY2569 LPPT→LLBG) מכיל את כל השדות הנדרשים. המשקלים שלו מומרים מליברות (86.183 ק"ג לנוסע), ולכן בדיקות העקביות של ADR-039 סובלות עיגול של 0.5%. כך זה מיושם גם בכלי הבדיקה.

## ADR-041 · מסך ההגדרות, ומימוש WP2 (מאשר את סקיצה 6)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **סקיצה:** `docs/mockups/s6-settings.html`
**החלטה (המשתמש):**
- **כל המקדמים ניתנים לעריכה**, מחולקים ל-10 קבוצות מקופלות. בכותרת של כל קבוצה מופיעה הנוסחה, וליד כל שדה ברירת המחדל שלו.
- **שמירה = גרסה חדשה של `rate_sets`.** לפני השמירה מוצגת טבלה של היה מול יהיה, עם הערה לא חובה. "חזור לגרסה" יוצר גרסה חדשה שמעתיקה את הישנה, ולא מוחק כלום.
- **שדה הבית: LLBG.**

**מימוש (החלטות טכניות):**
- **מקור אחד לברירות המחדל:** `web/db/rate-set-v1.json`. גם המיגרציה שזורעת את גרסה 1 וגם הקוד קוראים ממנו.
- **מיגרציות:** קבצי SQL ממוספרים ב-`web/db/migrations`, עם טבלת `schema_migrations`. הן רצות בזמן הבנייה ב-Vercel, רק אם מוגדר `DATABASE_URL`, ולכן אין שלב ידני.
- **הסיסמה נשארת ב-`APP_PASSWORD_HASH`** (משתנה סביבה). מ-`users` נשארים רק `failed_attempts` ו-`locked_until`, לנעילה קבועה. כך הנעילה עובדת גם בין מופעי שרת, והסיסמה לא נשמרת ב-DB.
- **מסד הנתונים נגיש רק מהשרת:** `@neondatabase/serverless` (HTTP), ורק מתוך קבצי `server-only`.
- **האתר לא נסרק:** `robots: noindex` ו-`X-Robots-Tag`. אתר פרטי לא צריך להופיע בחיפוש. **הרקע:** Chrome סימן את כתובת ה-preview כ-"Dangerous", כנראה בזיהוי שגוי של דף כניסה בדומיין חדש.

## ADR-042 · מנוע החישוב (WP3): פרטי מימוש
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מממש את:** ADR-025 עד 028, 034, 037, 039
- **מודול טהור:** `web/src/lib/engine`. אין בו DB, רשת או שעון. **הקלט:** טיסה וגרסת תעריפים. **הפלט:** שורות ספר חשבונות בסנטים חתומים, ולכל שורה `calc`.
- **עיגול:**
  - מחיר הכרטיס לנוסע מעוגל לדולר שלם, כמו בסקיצה 2.
  - כל שורה מעוגלת לסנט. חצי סנט מעוגל הרחק מאפס, כדי שהוצאה והכנסה באותו סכום יהיו סימטריות.
- **נייטרלי כשחסר נתון:** שעה מקומית לא ידועה (טיסות היסטוריות), מחיר EIA חסר, או דירוג "בבנייה", נותנים מכפיל 1.
- **שורות שנוצרות רק כשרלוונטי:**
  - עלויות GSX: רק אחרי שהוזנו.
  - מטען: רק כשהוא גדול מ-0.
  - קנס: רק כשהוא מעל 0.
  - הקפצה: רק מ-50 NM ומעלה.
  - הסטה: רק כשהייתה הסטה.
- **הקפצת צוות:** 2 טייסים, והדיילים לפי המושבים. טייסי תגבורת לא נספרים, כי הם נקבעים לפי אורך הטיסה החדשה.
- **EIA:**
  - הסדרה `EER_EPJK_PF4_RGC_DPG`: מחיר ספוט שבועי של דלק סילוני בחוף המפרץ, בדולר לגלון.
  - המרה לק"ג לפי צפיפות 0.80 ק"ג/ליטר, כלומר 3.03 ק"ג לגלון.
  - מטמון ב-`eia_prices` לשבוע. כש-EIA לא זמין, משתמשים בערך האחרון שנשמר.
- **בדיקות הזהב:**
  - הקוד של סקיצות 2 ו-3 מועתק לבדיקות כקוד ייחוס, ומושווה למנוע על יותר מ-4,000 צירופים. ההתאמה מדויקת עד הסנט.
  - הכיול על 10 הטיסות יוצא 20% (±0.5%) ב-K=1.51.

## ADR-043 · מחיר ייחוס הדלק = $1.51/ק"ג (מעדכן את ADR-025)
**תאריך:** 2026-09-25 · **סטטוס:** מאושר (המשתמש)
**רקע:** החיבור ל-EIA (ADR-042) החזיר מחיר של $1.5137 לק"ג בשבוע 2026-09-18, כלומר כ-$4.58 לגלון. זה פי 2 מהייחוס של $0.75. כך התוספת נתקעה על התקרה (+15%) בכל טיסה. הכרטיסים היו מתייקרים ב-15%, והשוליים היו עולים מ-20% לכ-30%, בניגוד לכיול. הכיול (ADR-034) נעשה עם תוספת נייטרלית.
**החלטה:**
- מחיר הייחוס עובר ל-**$1.51/ק"ג**, המחיר הנוכחי. היום התוספת היא 0%, השוליים נשארים 20%, והתוספת זזה רק עם שינוי במחיר מכאן והלאה.
- בגרסה החיה זה נכנס כ**גרסת תעריפים 2**, דרך מסך ההגדרות (ADR-021). ברירת המחדל ב-`rate-set-v1.json` עודכנה, כך שהתקנה חדשה מתחילה נכון.

## ADR-044 · מנוע המעקב (WP4): מימוש, וספים זמניים
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מממש את:** ADR-019, 020, 022, 024, 031
- **מכונת המצבים:** `tracker/src/machine.js`, קוד טהור שנבדק בהרצה חוזרת של טיסות מדומות. המצבים: `idle → armed → taxi_out → airborne → taxi_in → arrived`, ובנוסף `disconnected` ו-`interrupted`.
- **ספים זמניים עד שתהיה הקלטה של טיסה אמיתית** (המשתמש: "משהו דיפולטיבי, נדייק אחר כך"):

  | אירוע | הסף |
  |---|---|
  | OUT | תזוזה של 55 מ' מהגייט, או מהירות של 3 קשר ומעלה |
  | OFF | 60 קשר וגם 150 רגל מעל השדה, או 160 קשר |
  | ON | מתחת ל-40 קשר, אחרי שהמטוס היה באוויר |
  | IN | עצירה של 2 דקות רצופות, או ניתוק אחרי הנחיתה |
  | armed | על הקרקע, עד 5 NM מהמוצא |
- **התאמת OFP:** המוצא והיעד בתוכנית הטיסה ב-VATSIM זהים ל-OFP, או שאות הקריאה זהה. אחרי אישור (`ack`), אותו OFP לעולם לא יופעל שוב.
- **הצטרפות באמצע טיסה:** אם החיבור מתחיל כשהמטוס כבר באוויר, המעקב מתחיל משם, ואין OUT ו-OFF. זו טיסה `partial` (ADR-019).
- **VATSIM לא מספק סטטוס טיסה.** בדקנו את הפיד: יש בו מיקום, גובה, מהירות קרקע, כיוון, טרנספונדר ותוכנית טיסה, ואין שדה שלב. לכן הספים שלנו נחוצים.
- **D1:** שורה אחת עם המצב כ-JSON, ויומן מעברים שנמחק אחרי `ack`. לא נשמר מסלול (ADR-022).
- **דקה כפולה:** Cloudflare מריץ לפעמים את אותה דקה פעמיים (נצפה בשניה 27 ובשניה 39). בזמן טיסה פעילה כל דקה מעובדת פעם אחת בלבד (`last_tick`). אחרת חלון החזרה של 30 דקות היה מתקצר.
- **זמן מעבד אחרי הוספת D1: 4–8ms** מתוך 10ms. זה יותר מה-2–3ms שנמדדו ב-ADR-040. **למעקב:** אם תופיע חריגה (`exceededCpu`), מפסיקים לקרוא את הפיד אחרי מערך `pilots`.
- **API:**
  - `GET /v1/state` ו-`POST /v1/ack {ofp_id}` דורשים טוקן `v1.<exp>.<HMAC>`, עם תוקף של 10 דקות ולכל היותר שעה.
  - הטוקן חתום ב-`TRACKER_SECRET`, שמשותף ל-Vercel ול-Cloudflare. המשתמש מגדיר אותו בעצמו.
  - CORS מותר רק לכתובת האפליקציה.
- **SimBrief ID ו-CID** מוגדרים כמשתנים ב-`wrangler.toml`. ה-Worker לא קורא את Neon, ולכן שינוי שלהם במסך ההגדרות מחייב גם פריסה של ה-Worker. זה מקרה נדיר.

## ADR-045 · מסך הטיסה (WP5): מימוש, והבדלים מסקיצה 1a
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מממש את:** סקיצה 1a, ADR-019, 020, 021, 025, 027
- **דף אחד עם 6 מצבים**, והשרת בוחר ביניהם לפי המקורות האלה:
  - מצב ה-Worker.
  - ה-OFP האחרון ב-SimBrief (טרי, עד 12 שעות, וטרם נרשם).
  - הלוגבוק.
  - הפרמטר `?manual=1` פותח את הטופס הידני, בשביל "טיסה ידנית" ו"סיים ידנית".
- **מצב חי:** `TrackerProvider` קורא ישירות מה-Worker כל 20 שניות, עם טוקן של 10 דקות (ADR-024). כשהמצב משתנה, הדף מבקש מהשרת לחשב מחדש. נורית החיבור בסרגל העליון נדלקת לפי "נראה ב-VATSIM ב-2.5 הדקות האחרונות".
- **טופס ההשלמה:**
  - ספר החשבונות מחושב בדפדפן עם אותו מנוע, דרך `flight-input.ts`.
  - בסגירה השרת בונה הכול מחדש. זמנים שנמדדו נעולים. הטיסה ושורות ספר החשבונות נכתבות **בפקודת SQL אחת**, והודות ל-`ON CONFLICT (ofp_id)` אין כפילות.
  - אחר כך האפליקציה שולחת `ack` ל-Worker. הוא מקבל `ack` מכל מצב שאינו `idle`, כדי ש"סיים ידנית" יעבוד גם באמצע טיסה.
- **גרסת התעריפים והדלק לפי OUT:** הגרסה החדשה ביותר שנוצרה לפני OUT, ומחיר EIA של השבוע של OUT.
- **השדה בפועל:** השדה הקרוב לנקודת הנחיתה. אם הוא לא היעד, זו הסטה.
- **הזנת זמן ידנית:** בפורמט HH:MM, מעוגנת לזמן הקודם. זמן שעובר לתאריך הבא מסומן "‎+1 יום".
- **הבדלים מהסקיצה, ובאיזה שלב הם ייסגרו:**
  - **5 שלבים במקום 8:** בגייט, הסעה, באוויר, הסעה לגייט ובחניה. ה-Worker לא מבחין בין טיפוס, שיוט והנמכה. אפשר להוסיף את זה אחרי הכיול.
  - **אין כפתור "בטל תוכנית".** תוכנית שלא נטסה מוחלפת בתוכנית חדשה או פגה אחרי 12 שעות (ADR-020).
  - **טיסה ידנית לגמרי נרשמת ליעד המתוכנן.** אין לה נקודת נחיתה, ולכן לא מזוהה בה הסטה.
  - **"אבן הדרך הבאה" ודירוג המוניטין** יגיעו ב-WP7. עד אז הדירוג נייטרלי (ADR-037).
- **`/dev-preview`:** כל המצבים עם נתונים מדומים, לבדיקה ויזואלית בלי DB. מחזיר 404 בסביבת Production.

## ADR-046 · הלוגבוק (WP6): מימוש
**תאריך:** 2026-09-25 · **סטטוס:** מאושר · **מממש את:** סקיצה 1b, ADR-013, 021, 022, 023
- **הסינון רץ בדפדפן, על כל הלוגבוק שנטען פעם אחת.** יש משתמש אחד וכמה מאות טיסות, אז התגובה מיידית.
- **פונקציית סינון אחת** (`logbook-filter.ts`) משמשת את הרשימה, את המפה ואת ייצוא ה-Excel (ADR-023). הסינון נשמר בכתובת הדף, וקישור הייצוא נושא את אותם פרמטרים בדיוק.
- **עריכה (ADR-021):** אפשר לערוך רק סכומי GSX, FPM, וזמנים שסומנו `m`. החישוב מחדש משתמש **בצילום של הטיסה**: ה-OFP, גרסת התעריפים, מחיר EIA והדירוג, כפי שהיו בסגירה. העדכון, המחיקה של השורות הישנות וההוספה של החדשות נעשים בפקודה אחת, ואחר כך הטיסה מסומנת `edited_at`. טיסות היסטוריות לא ניתנות לעריכה.
- **מחיקה:** מוחקת את הטיסה ואת השורות שלה (cascade), אחרי אישור.
- **מפה (ADR-022):**
  - הספריות `d3-geo`, `world-atlas` (Natural Earth 1:50m) ו-`topojson`, כולן בתוך האתר, בלי שרת אריחים.
  - קשתות Great Circle. העובי לפי מספר הטיסות, ומסלול בהפסד באדום.
  - המפה נשארת LTR גם בממשק RTL.
- **Excel (ADR-013):** הקובץ כולל גליון קריא, וגליון `_backup` מוסתר (`veryHidden`) עם השורות המלאות, חתום ב-HMAC שנגזר מ-`SESSION_SECRET`. השחזור:
  - דוחה קובץ ששונה, קובץ ממערכת אחרת או גליון רגיל.
  - משחזר רק טיסות שחסרות, לפי `ofp_id` או `legacy_doc._id`.
  - לעולם לא יוצר טיסה שלא יוצאה.
- **מגבלת גוף לפעולות שרת: 4MB** (ברירת המחדל היא 1MB), בשביל העלאת הגיבוי. Vercel מגביל ל-4.5MB.
- **`uuid` נכפה ל-^11** (דרך `overrides`), בגלל חולשה בגרסה שמגיעה עם `exceljs`.
- **בדיקות:** הבדיקות רצות עם `--conditions=react-server`, כדי ש-`server-only` יטען כקובץ ריק, ועם טוען שמשלים ייבוא בלי סיומת (`test/_hooks.mjs`).
