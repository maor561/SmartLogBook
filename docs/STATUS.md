# STATUS — SmartLogBook 2.0

**עודכן:** 2026-09-25

## מצב נוכחי
שלב 0: אפיון. אין קוד חדש. המערכת הישנה ב-`main` חיה וממשיכה לעבוד.
העבודה מתנהלת בענף `makeover`.

## מה נעשה
- 27 החלטות (ADR-001 עד ADR-027). ADR-019 מחליף את ADR-003.
- `PROJECT_BRIEF.md`: שאלות Q1, Q2, Q3, Q4, Q7, Q8 נסגרו.
- סקיצות שאושרו:
  - **0** `s0-visual-direction.html`: כיוון חזותי תפעולי / EFB.
  - **1a** `s1-flight-screen.html`: מחזור החיים ומסך "טיסה" ב-6 מצבים.
  - **1b** `s1b-logbook-analysis.html`: לוגבוק (רשימה, פרטים, מפה) ושלד של מסך הניתוח.
  - **2** `s2-dynamic-pricing.html`: מחיר כרטיס דינמי (ADR-025).
- Q3: מנוע המעקב ב-Cloudflare Worker + D1 (ADR-024). המגבלות אומתו מול התיעוד ונמדדו על נתונים אמיתיים.
- Q2: נוסחאות הקפצת צוות והסטה (ADR-026, ADR-027).

## ממתין לאישור
- **סקיצה 3** `s3-cost-engine.html`: מנוע ההוצאות האוטומטיות (Q9). צריך גם החלטה על **כיול הרווחיות**: בטיסה קצרה ב-737 השוליים יוצאים 66%, ובטיסה ארוכה ב-777 הם יוצאים 15%.

## הבא בתור
1. סגירת Q9 וכיול הרווחיות.
2. שלב 4: מודל הנתונים (ספר חשבונות, צילום תעריפים), מאגר שדות (Q10), העברת הטיסות הישנות (Q5).
3. שלב 5: המלצה על דוחות, סטטיסטיקות, אבני דרך ודירוג (Q6).
4. שלב 6: חבילות עבודה, סדר בנייה, ובדיקה ראשונה של זמן המעבד ב-Cloudflare. נדרש חשבון Cloudflare של המשתמש.

## שאלות פתוחות
ראה `PROJECT_BRIEF.md` §9: Q5, Q6, Q9, Q10.

## יומן סשנים
| תאריך | מה נעשה |
|---|---|
| 2026-09-25 | פתיחת הפרויקט. סבבי שאלות, 27 ADRs, סקיצות 0 עד 3, מחקר על מנוע המעקב. |
