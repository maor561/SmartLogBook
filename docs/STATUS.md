# STATUS — SmartLogBook 2.0

**עודכן:** 2026-09-25

## מצב נוכחי
**האפיון הושלם ותוכנית העבודה אושרה.** כל 11 השאלות הפתוחות נסגרו ב-38 החלטות. **בעבודה: WP0.**
אין עדיין קוד חדש. המערכת הישנה ב-`main` חיה, והעבודה מתנהלת בענף `makeover`.

## מה נעשה
- **40 החלטות** (ADR-001 עד ADR-040). ADR-019 מחליף את ADR-003, ו-ADR-034 מתקן את K מ-ADR-029.
- **מסמכים:** `PROJECT_BRIEF.md`, `DECISIONS.md`, `DATA_MODEL.md` v0.1, `WORK_PACKAGES.md` v0.1.
- **סקיצות מאושרות** (`docs/mockups/`):
  - s0: כיוון חזותי.
  - s1a: מסך טיסה.
  - s1b: לוגבוק.
  - s2: מחיר כרטיס.
  - s3: מנוע ההוצאות.
  - s4: מודל הנתונים.
  - s5: ניתוח.
- **כיול** על 10 טיסות אמיתיות: K=1.51, יעד שוליים 20% (ADR-034).
- **מחקר:** מנוע המעקב ב-Cloudflare Worker + D1, עם מגבלות שאומתו (ADR-024).

## הבא בתור
1. **WP0**, בדיקות סיכון:
   - ✅ **זמן מעבד בענן: 2–3ms מתוך 10ms** (ADR-040). ה-Worker חי ורץ כל דקה.
   - ✅ **OFP של המשתמש:** כל השדות קיימים (ADR-039, 040).
   - ⏳ **הקלטת טיסה אמיתית** לקביעת ספי השלבים: `node tools/record-flight.mjs --cid 1242058`, בטיסה הבאה של המשתמש.
2. **WP1 (שלד): אושר ובעבודה.** בזמן הפיתוח הוא יושב בפרויקט Vercel נפרד (`smartlogbook-v2`), כדי לא לפגוע באתר החי. במעבר (WP8) הוא עובר לכתובת הקיימת.

## בעיות פתוחות
- ~~ה-API של המערכת הישנה פתוח לכולם~~ ✅ **נסגר ב-25.09** (`52b93ca`, ב-`main`). נוסף דף כניסה עם סיסמה (משתנה `SITE_PASSWORD` ב-Vercel) ועוגייה ל-90 יום. זה פתרון ביניים עד WP8.

## יומן סשנים
| תאריך | מה נעשה |
|---|---|
| 2026-09-25 | פתיחת הפרויקט ואפיון מלא: 38 ADRs, 7 סקיצות, מחקר מעקב, כיול על נתונים אמיתיים, ותוכנית עבודה. |
