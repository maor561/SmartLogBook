# Copy to .env.local for local dev. In Vercel, set these as Environment Variables.
# Session signing key (32+ random chars):  node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=
# Password hash (ADR-031):  node scripts/hash-password.mjs
APP_PASSWORD_HASH=
