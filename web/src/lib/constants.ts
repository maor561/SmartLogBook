// Shared by the proxy and the server code. Kept free of server-only imports
// so the proxy bundle stays small.
export const SESSION_COOKIE = 'slb_session';
export const SESSION_DAYS = 90;
export const THEME_COOKIE = 'slb_theme';
